import { a as Session } from './SessionManager-Ch97i4lf.js';
import 'hono/utils/cookie';
import 'hono';

declare module 'hono' {
    interface ContextVariableMap {
        session: Session;
    }
    interface Context<E> {
        get session(): 0 extends (1 & E) ? Session : E extends {
            Variables: {};
        } ? E['Variables']['session'] : Session;
        set session(value: null);
    }
}
